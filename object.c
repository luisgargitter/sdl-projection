/*
#include "object.h"

#include <stdlib.h>
#include <stdint.h>
#include <stdbool.h>

struct membcount {
    int32_t v; // amount of vertices
    int32_t vt; // amount of vertex texture
    int32_t vn; // amount of vertex normals
    int32_t f; // amount of surfaces
};

bool isSubstring(char *st_v, int32_t st_c, char ss_v, int32_t ss_c) {
    for(int32_t i = 0; i <= st_c - ss_c; i++) {
        for(int32_t j = 0; j < ss_c; j++) {
            if(ss_v[j] = st_v[
        }
    }
}

struct membcount countMembers(int32_t buf_c, char* buf_v) {
    struct membcount m = {0, 0, 0, 0};
    
    for(int32_t i = 0; i < buf_c; i++) {
        switch() {
            
        }
    }

} 


object_t * objectNew(int32_t buf_c, char* buf_v) {
    object_t* obj = malloc(sizeof(obj));

}

*/
